services:
  - type: web
    name: weight-management-backend
    env: java
    rootDir: backend/weight-management
    buildCommand: mvn clean package
    startCommand: java -jar target/weight-management-0.0.1-SNAPSHOT.jar
    plan: free